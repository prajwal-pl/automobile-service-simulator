import React from "react";
import { redirect } from "next/navigation";

type Props = {};

const Home = async (props: Props) => {
  return (
    <div>
      <div>Home</div>
    </div>
  );
};

export default Home;
